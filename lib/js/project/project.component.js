// Register `project` component, along with its associated controller and template
angular.
  module('project').
  component('project', {  // This name is what AngularJS uses to match to the `<employee>` element.
    templateUrl: 'lib/js/project/project.template.html',
  });
